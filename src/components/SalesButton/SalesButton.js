import React from "react";
import classes from "./SalesButton.module.css";

const salesButton = () => {
	return <button className={classes.SalesButton}>Contact Sales</button>;
};

export default salesButton;
